services:
  superds:
    image: python:3.11-slim
    working_dir: /app
    command: sh -c "pip install -r requirements.txt && python -m backend.app"
    ports:
      - "8787:8787"
    volumes:
      - .:/app
    env_file:
      - .env
